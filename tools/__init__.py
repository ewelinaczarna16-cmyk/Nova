"""Nova tools: the hands. One concern per file, registered — never edited into the
core. The registry is handed to the model whole each turn."""
from .registry import Tool, ToolRegistry, ToolError, ToolResult  # noqa: F401
